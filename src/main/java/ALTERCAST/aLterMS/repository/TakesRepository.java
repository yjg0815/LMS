package ALTERCAST.aLterMS.repository;

import ALTERCAST.aLterMS.domain.Takes;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TakesRepository extends JpaRepository<Takes, Long> {
}
